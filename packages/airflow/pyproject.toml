[project]
name = "airflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "vitormbesen" }
]
requires-python = ">=3.14"
dependencies = [
    "medallion-rag>=0.1.0",
]

#[build-system]
#requires = ["hatchling"]
#build-backend = "hatchling.build"

[tool.uv.sources]
medallion-rag = { path = "../medallion-rag", editable = true }

