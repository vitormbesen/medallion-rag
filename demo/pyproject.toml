[project]
name = "demo"
version = "0.1.0"
description = "Demo for querying the Postgres vector database"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pgvector>=0.5.0",
    "psycopg2-binary>=2.9.12",
    "sqlalchemy>=2.0.51",
    "medallion-rag>=0.1.0",
]


[tool.uv.sources]
medallion-rag = { path = "../packages/medallion-rag"}